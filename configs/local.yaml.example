# 本地配置模板
# 复制此文件为 local.yaml 并填入实际配置

dataset:
  name: "mvtec"
  data_dir: "./data/mvtec"
  image_size: 224

llm:
  api_key: "your-api-key-here"  # 替换为实际API Key

# 设备配置
device:
  type: "cuda"  # cuda 或 cpu
